version: "3"
services:
  watchtower:
    image: nickfedor/watchtower:1.21.2
    restart: always
    command:
      - --cleanup=true
      - --interval=300
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock