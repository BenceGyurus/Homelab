version: '3.8'

services:
  wifi-densepose:
    image: ruvnet/wifi-densepose:latest
    container_name: ruview_server
    ports:
      - "3000:3000"
    environment:
      - SENSING_ALLOWED_HOSTS=192.168.1.39,192.168.1.39:3000
    restart: unless-stopped